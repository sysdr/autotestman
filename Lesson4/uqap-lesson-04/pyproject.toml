[project]
name = "uqap-lesson-04-data-parsing"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.0.0",
    "openpyxl>=3.1.0",
    "pytest>=7.4.0",
    "flask>=3.0.0",
]

[project.optional-dependencies]
dev = [
    "mypy>=1.5.0",
]
